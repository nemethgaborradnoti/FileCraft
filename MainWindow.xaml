<Window x:Class="FileCraft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileCraft"
        xmlns:views="clr-namespace:FileCraft.Views"
        xmlns:viewModels="clr-namespace:FileCraft.ViewModels"
        mc:Ignorable="d"
        Title="FileCraft" Height="800" Width="850"
        MinHeight="200" MinWidth="250"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,0,5,5">
            <Button Style="{StaticResource BorderlessIconButton}" ToolTip="Undo" Command="{Binding UndoCommand}" IsEnabled="{Binding CanUndo}">
                <Image Source="{StaticResource IconUndo}" />
            </Button>
            <Button Style="{StaticResource BorderlessIconButton}" ToolTip="Redo" Command="{Binding RedoCommand}" IsEnabled="{Binding CanRedo}">
                <Image Source="{StaticResource IconRedo}" />
            </Button>
            <Button Style="{StaticResource BorderlessIconButton}" ToolTip="Save current settings" Command="{Binding SaveCommand}" IsEnabled="{Binding HasUnsavedChanges}">
                <Image Source="{StaticResource IconSave}" />
            </Button>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Source Folder" Margin="0,0,5,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Margin="0,0,10,0"
                             Style="{StaticResource ModernPathTextBoxStyle}"
                             Text="{Binding SourcePath, Mode=OneWay}"
                             ToolTip="{Binding SourcePath}"/>

                    <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectSourcePathCommand}"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Destination Folder" Margin="5,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Margin="0,0,10,0"
                             Style="{StaticResource ModernPathTextBoxStyle}"
                             Text="{Binding DestinationPath, Mode=OneWay}"
                             ToolTip="{Binding DestinationPath}"/>

                    <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectDestinationPathCommand}"/>
                    <Button Grid.Column="2" Content="Clear" Command="{Binding ClearPathsCommand}" ToolTip="Clear both source and destination paths." Style="{StaticResource WarningButton}"/>
                </Grid>
            </GroupBox>
        </Grid>

        <TabControl Grid.Row="2" Margin="5" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource IconFileContent}" Width="28" Height="28" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="File Content Export" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:FileContentExportView DataContext="{Binding FileContentExportVM}"/>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource IconTreeStructure}" Width="28" Height="28" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Tree Generator" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:TreeGeneratorView DataContext="{Binding TreeGeneratorVM}"/>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource IconFolderContent}" Width="28" Height="28" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Folder Content Export" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:FolderContentExportView DataContext="{Binding FolderContentExportVM}"/>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource IconSettings}" Width="28" Height="28" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Options" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:OptionsView DataContext="{Binding OptionsVM}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
