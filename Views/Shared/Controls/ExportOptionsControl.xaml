<UserControl x:Class="FileCraft.Views.Shared.Controls.ExportOptionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="400">
    <GroupBox Header="{DynamicResource ExportOptions_Header}" Padding="5" Margin="0,0,10,0">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{DynamicResource ExportOptions_FileNameLabel}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding OutputFileName, UpdateSourceTrigger=PropertyChanged}"
                     Width="150"
                     VerticalAlignment="Center"
                     Style="{StaticResource ModernTextBoxStyle}"/>
            <TextBlock Text=".txt" VerticalAlignment="Center" Margin="2,0,10,0" Foreground="Gray"/>
            <CheckBox IsChecked="{Binding AppendTimestamp}" VerticalAlignment="Center">
                <TextBlock Text="{DynamicResource ExportOptions_AppendTimestampLabel}" ToolTip="{DynamicResource ExportOptions_AppendTimestampTooltip}"/>
            </CheckBox>
        </StackPanel>
    </GroupBox>
</UserControl>