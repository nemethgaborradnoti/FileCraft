<Window x:Class="FileCraft.Views.Shared.PathPresetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:FileCraft.ViewModels.Functional"
        xmlns:helpers="clr-namespace:FileCraft.Shared.Helpers"
        xmlns:icons="clr-namespace:Fonts"
        mc:Ignorable="d"
        Title="{DynamicResource PathPreset_Title}"
        Height="450" Width="700"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        MinHeight="300" MinWidth="500">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource PathPreset_Title}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Background="{StaticResource TextBoxBackgroundBrush}">
            <ListBox ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="{StaticResource GrayTextBrush}" FontSize="11"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding FilePaths.Count, StringFormat='({0})'}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{StaticResource GrayTextBrush}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,0,0,0">
            <Button Content="{DynamicResource PathPreset_SaveCurrentButton}" Command="{Binding SaveCurrentCommand}" Style="{StaticResource SuccessButton}" Margin="0,0,0,10" MinWidth="180"/>
            <Separator Margin="0,5,0,15"/>

            <Button Content="{DynamicResource PathPreset_LoadButton}" Command="{Binding LoadSelectedCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,10"/>
            <Button Content="{DynamicResource PathPreset_ViewButton}" Command="{Binding ViewContentCommand}" Margin="0,0,0,10"/>
            <Button Content="{DynamicResource Common_DeleteButton}" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" Margin="0,0,0,10"/>
            <Button Content="{DynamicResource RenamePreset_Title}" Command="{Binding RenameCommand}" Margin="0,0,0,10"/>
        </StackPanel>

        <Button Grid.Row="2" Grid.Column="1" Content="{DynamicResource Common_CloseButton}" Command="{Binding CloseCommand}" Style="{StaticResource SecondaryButton}" Margin="15,15,0,0" HorizontalAlignment="Right" MinWidth="100"/>
    </Grid>
</Window>