<Window x:Class="FileCraft.Views.Shared.PathPresetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:FileCraft.ViewModels.Functional"
        xmlns:controls="clr-namespace:FileCraft.Views.Shared.Controls"
        mc:Ignorable="d"
        Title="{DynamicResource PathPreset_Title}"
        Height="550" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        MinHeight="400" MinWidth="350">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource PathPreset_Title}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <controls:PresetListControl Grid.Row="1" DataContext="{Binding ListViewModel}" />
    </Grid>
</Window>