<Window x:Class="FileCraft.Views.Shared.IgnoredCommentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select files to ignore '///' comments"
        Width="600" Height="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        MinHeight="300" MinWidth="400">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select the files where XML comments ('///') should be ignored during export." 
                   TextWrapping="Wrap" Margin="0,0,0,10"/>

        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <ListView x:Name="FilesListView" ItemsSource="{Binding Files}" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ItemContainerStyle="{StaticResource SharedListViewItemStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center">
                            <TextBlock Text="{Binding RelativePath}" TextTrimming="CharacterEllipsis"/>
                        </CheckBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" IsDefault="True" Click="SaveButton_Click" MinWidth="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" MinWidth="80" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>