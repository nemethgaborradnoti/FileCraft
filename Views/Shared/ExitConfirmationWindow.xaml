<Window x:Class="FileCraft.Views.Shared.ExitConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm Exit"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        MinWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="pack://application:,,,/Resources/warning01.png" Width="48" Height="48" Margin="0,0,15,0" VerticalAlignment="Center"/>

            <TextBlock x:Name="MessageTextBlock"
                       Grid.Column="1"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Click="SaveButton_Click" IsDefault="True" MinWidth="90" Margin="0,0,10,0"/>
            <Button Content="Don't Save" Click="DontSaveButton_Click" MinWidth="90" Margin="0,0,10,0" Style="{StaticResource DangerButton}"/>
            <Button Content="Cancel" Click="CancelButton_Click" IsCancel="True" MinWidth="90" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
