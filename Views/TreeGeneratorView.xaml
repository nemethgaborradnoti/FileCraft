<UserControl x:Class="FileCraft.Views.TreeGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileCraft.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Source Folder Selection -->
        <Label Grid.Row="0" Grid.Column="0" Content="Folder to generate the structure from:" FontWeight="SemiBold"/>
        <TextBox x:Name="sourcePathTextBox" Grid.Row="1" Grid.Column="0" Margin="0,0,10,0" IsReadOnly="True" Background="#eee"/>
        <Button x:Name="sourceBrowseButton" Grid.Row="1" Grid.Column="1" Content="Browse..." Padding="10,2" Click="SourceBrowseButton_Click"/>

        <!-- Destination Folder Selection -->
        <Label Grid.Row="3" Grid.Column="0" Content="Where to save the .txt file?" FontWeight="SemiBold"/>
        <TextBox x:Name="destinationPathTextBox" Grid.Row="4" Grid.Column="0" Margin="0,0,10,0" IsReadOnly="True" Background="#eee"/>
        <Button x:Name="destinationBrowseButton" Grid.Row="4" Grid.Column="1" Content="Browse..." Padding="10,2" Click="DestinationBrowseButton_Click"/>

        <!-- Exclude Folders -->
        <Label Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="Folders to exclude (separated by ';' semicolons):" FontWeight="SemiBold"/>
        <TextBox x:Name="excludeFoldersTextBox" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,0" ToolTip="For example: .git;node_modules;bin;obj"/>

        <!-- Action Button -->
        <Button x:Name="generateButton" Grid.Row="8" Grid.Column="0" Content="Generate Structure (.txt)" Padding="10,5" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontWeight="Bold" Click="GenerateButton_Click"/>

    </Grid>
</UserControl>
