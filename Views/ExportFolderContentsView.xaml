<UserControl x:Class="FileCraft.Views.ExportFolderContentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileCraft.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Options -->
        <CheckBox x:Name="includeSubfoldersCheckBox" Grid.Row="0" Grid.Column="0" Content="Include files in subfolders" IsChecked="False" Margin="0,5,0,0"/>

        <!-- Action Button -->
        <Button x:Name="exportButton" Grid.Row="1" Grid.Column="0" Content="Export Contents (.txt)" Padding="10,5" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontWeight="Bold" Click="ExportButton_Click"/>

    </Grid>
</UserControl>
