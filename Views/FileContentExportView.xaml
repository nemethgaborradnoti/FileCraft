<UserControl x:Class="FileCraft.Views.FileContentExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileCraft.Views"
             xmlns:viewmodels="clr-namespace:FileCraft.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Section for folder and extension selection -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Folders to Include -->
            <GroupBox Grid.Column="0" Header="Folders to Include" Margin="5">
                <TreeView ItemsSource="{Binding RootFolders}" Height="250" BorderThickness="0">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="2"/>
                            <!-- This removes the dotted focus rectangle -->
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Resources>
                                <!-- Override the selection and highlight brushes to make selection invisible -->
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                            </Style.Resources>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" IsThreeState="True" VerticalAlignment="Center" Content="{Binding Name}"
                                      PreviewMouseDown="FolderTreeCheckBox_PreviewMouseDown"/>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <!-- File extensions to include -->
            <GroupBox Grid.Column="1" Header="File extensions to include" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="4,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" Name="ExtensionsHeaderCheckBox" Checked="OnCheckAllExtensions" Unchecked="OnCheckAllExtensions" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="Extension" FontWeight="SemiBold" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- List -->
                    <ListView Grid.Row="1" ItemsSource="{Binding AvailableExtensions}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Height="250">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="4,1"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Section for selected files -->
        <GroupBox Grid.Row="1" Header="Files to Export" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="4,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Name="HeaderCheckBox" Checked="OnCheckAll" Unchecked="OnCheckAll" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="File Path" FontWeight="SemiBold" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- List -->
                <ListView Grid.Row="1" ItemsSource="{Binding SelectableFiles}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="4,1"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center">
                                <TextBlock Text="{Binding FullPath}" TextTrimming="CharacterEllipsis"/>
                            </CheckBox>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </GroupBox>

        <!-- Export Options and Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">

            <!-- File Counters -->
            <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                <TextBlock>
                    <Run Text="Available files:" FontWeight="SemiBold"/>
                    <Run Text="{Binding AvailableFilesCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="Selected files:" FontWeight="SemiBold"/>
                    <Run Text="{Binding SelectedFilesCount, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>

            <GroupBox Header="Export Options" Padding="5" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="File Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding OutputFileName, UpdateSourceTrigger=PropertyChanged}" Width="150" VerticalAlignment="Center"/>
                    <TextBlock Text=".txt" VerticalAlignment="Center" Margin="2,0,10,0" Foreground="Gray"/>
                    <CheckBox IsChecked="{Binding AppendTimestamp}" VerticalAlignment="Center">
                        <TextBlock Text="Append Timestamp" ToolTip="Adds _yyyy_MM_dd_HH_mm_ss to the file name."/>
                    </CheckBox>
                </StackPanel>
            </GroupBox>
            <Button Content="Export Selected File Contents" 
                    Command="{Binding ExportFileContentCommand}" 
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
