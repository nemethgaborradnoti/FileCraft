<UserControl x:Class="FileCraft.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileCraft.Views"
             xmlns:viewmodels="clr-namespace:FileCraft.ViewModels.Functional"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="10" VerticalAlignment="Top">
            <GroupBox Header="Load Settings Preset" Padding="10">
                <ItemsControl ItemsSource="{Binding LoadPresetSlots}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding PresetNumber}" 
                                    Command="{Binding DataContext.LoadPresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding PresetNumber}" 
                                    Width="50" Height="30" Margin="5">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Background" Value="#FFE6E6E6"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Exists}" Value="True">
                                                <Setter Property="Background" Value="#7a9cc7"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <GroupBox Header="Save Settings Preset" Padding="10" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="1" Command="{Binding SavePresetCommand}" CommandParameter="1" Width="50" Height="30" Margin="5"/>
                    <Button Content="2" Command="{Binding SavePresetCommand}" CommandParameter="2" Width="50" Height="30" Margin="5"/>
                    <Button Content="3" Command="{Binding SavePresetCommand}" CommandParameter="3" Width="50" Height="30" Margin="5"/>
                    <Button Content="4" Command="{Binding SavePresetCommand}" CommandParameter="4" Width="50" Height="30" Margin="5"/>
                    <Button Content="5" Command="{Binding SavePresetCommand}" CommandParameter="5" Width="50" Height="30" Margin="5"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding Version, StringFormat='Version: {0}'}"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,0,10,10"/>
    </Grid>
</UserControl>
